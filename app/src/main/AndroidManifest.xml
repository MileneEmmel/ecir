<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ecir"
        tools:targetApi="31">
        <!-- Tela principal ao abrir o app -->
        <activity android:name=".MenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Outras telas -->
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".DadosPessoaisActivity" />
        <activity android:name=".CertificadosActivity" />
        <activity android:name=".Embarque" />
        <activity android:name=".CalendarioActivity" />
        <activity android:name=".NovoCertificadoActivity" />
        <activity android:name=".ConfiguracoesActivity" />
        <activity android:name=".NovoRegistroEmbarqueActivity" />
        <activity android:name=".RegistroEmbarqueActivity" />
        <activity android:name=".ItemCertificadoActivity" />
        <activity android:name=".Certificado" />
        <activity android:name=".EmbarqueAdapter" />
        <activity android:name=".DetalhesEmbarqueActivity" />
        <activity android:name=".EditarEmbarqueActivity" />
        <activity android:name=".ItemEmbarqueAdapter" />
        <activity android:name=".DadosPessoais" />




    </application>

</manifest>